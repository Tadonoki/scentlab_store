/**
 * Seed database dengan menjalankan:
 * 1. Pastikan dev server berjalan: npm run dev
 * 2. Buka http://localhost:3000/api/seed
 * 
 * Atau jalankan langsung: curl http://localhost:3000/api/seed
 *
 * Script ini akan membuat:
 * - 7 produk awal (Sweet Dreams, Calm Horizon, dll)
 * - 1 admin user (admin@scentlab.com / admin123456)
 */

// Gunakan endpoint API /api/seed untuk menjalankan seeding
// karena password hashing perlu runtime Better Auth
export {};